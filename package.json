{
  "name": "dummy-llm-api",
  "version": "0.1.0",
  "description": "Dummy LLM API server. Currently supports the Anthropic Messages API format, designed to be extended with OpenAI support. Streams infinite simulated responses with random thinking blocks.",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "build": "tsc"
  },
  "dependencies": {
    "express": "^5.2.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
